# 版本与兼容性政策（VERSIONING）

> 本文件是开放AIoT（Open AIoT）全部规范的版本纪律，与总纲 §7 一致。任何规范文档、参考实现的发布都必须遵守。

## 1. 语义化版本

所有规范采用 `MAJOR.MINOR.PATCH`。`v0.x` 为 Draft 阶段，允许快速迭代（每次变更仍须记录于文档变更记录）；**兼容性承诺自 1.0 起生效**。

## 2. 主版本内只增不删（additive-only）

1.0 之后，同一 MAJOR 内只允许新增字段/能力/可选参数；禁止删除、禁止改变已有字段语义、禁止收窄类型。

## 3. 必填字段冻结

任何已发布规范中的必填字段，永不改名、永不改义、永不改型。新需求以可选字段进入。

## 4. 弃用流程

标记 `deprecated` → 至少保留一个完整 MINOR 周期并公示 → 方可在下一 MAJOR 移除。

## 5. 前向兼容义务（must-ignore）

所有符合规范的实现**必须忽略未知字段**，使旧实现能读新数据。规范文本中必须显式写出这条义务。

## 6. 厂商扩展命名空间

扩展字段以 `x-<vendor>.` 前缀隔离；扩展字段永不与标准字段冲突；成功的扩展可经 RFC 流程收编为标准字段。

## 7. 版本协商内建

协议握手/发现期进行版本声明与协商；每份规范文档头部携带 `specVersion`。

## 8. 破坏性变更的唯一通道

破坏性变更必须走 RFC 流程（见 rfcs/ 目录与治理文件），含公开公示期；任何人（含芯步内部）无特权捷径。

---

*本政策的修改本身视同破坏性变更，须走 RFC 流程。*
